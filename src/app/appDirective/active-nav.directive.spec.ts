import { ActiveNavDirective } from './active-nav.directive';

describe('ActiveNavDirective', () => {
  it('should create an instance', () => {
    const directive = new ActiveNavDirective();
    expect(directive).toBeTruthy();
  });
});
