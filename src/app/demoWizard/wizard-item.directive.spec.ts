import { WizardItemDirective } from './wizard-item.directive';

describe('WizardItemDirective', () => {
  it('should create an instance', () => {
    const directive = new WizardItemDirective();
    expect(directive).toBeTruthy();
  });
});
